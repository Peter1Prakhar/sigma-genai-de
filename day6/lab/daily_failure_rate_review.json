{
  "file": "queries/daily_failure_rate.sql",
  "reviewed_at": "2026-05-25T18:47:12.431129",
  "issues_found": 3,
  "review": {
    "issues": [
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Incorrect calculation of failure rate",
        "line": "failed_count / total_count * 100 as failure_rate",
        "problem": "The failure rate is calculated incorrectly because failed_count is not defined in the SELECT clause but used in the calculation.",
        "fix": "failed_count / total_count * 100 as failure_rate"
      },
      {
        "severity": "Medium",
        "category": "Performance",
        "title": "Correlated subquery",
        "line": "SELECT COUNT(*) FROM fact_transactions f2 WHERE f2.status = 'FAILED' AND f2.transaction_date = t.transaction_date",
        "problem": "The correlated subquery can be inefficient as it runs for each row in the outer query.",
        "fix": "LEFT JOIN (SELECT transaction_date, COUNT(*) as failed_count FROM fact_transactions WHERE status = 'FAILED' GROUP BY transaction_date) f2 ON f2.transaction_date = t.transaction_date"
      },
      {
        "severity": "Low",
        "category": "Readability",
        "title": "Lack of comments",
        "line": "Entire query",
        "problem": "The query lacks comments explaining the logic, which makes it harder to understand.",
        "fix": "Add comments explaining the purpose of each part of the query."
      }
    ],
    "corrected_sql": "SELECT t.transaction_date, COALESCE(f2.failed_count, 0) as failed_count, COUNT(*) as total_count, COALESCE(f2.failed_count, 0) * 100.0 / COUNT(*) as failure_rate FROM fact_transactions t LEFT JOIN (SELECT transaction_date, COUNT(*) as failed_count FROM fact_transactions WHERE status = 'FAILED' GROUP BY transaction_date) f2 ON f2.transaction_date = t.transaction_date GROUP BY t.transaction_date ORDER BY t.transaction_date;",
    "summary": "The query has a critical correctness issue with the failure rate calculation and a performance issue with the correlated subquery. It also lacks comments for readability."
  }
}